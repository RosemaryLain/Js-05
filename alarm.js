function setAlarm (){
    let  seconds = parseInt(prompt("..."));
    if (isNaN) (seconds) || seconds <= 0) {
        console.log("");
        return;
    }
    setTimeout(())=>{
        console.log("Time to wake up after "+seconds + "seconds.");

    }
    setAlarm();
}